import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#607D8B'
        },
        secondary: {
            main: '#757575'
        }
    },
    typography: {
        fontFamily: 'main, Arial'
    }
});